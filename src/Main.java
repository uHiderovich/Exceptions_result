import controller.PersonNotebookController;

public class Main {
    public static void main(String[] args) {
        PersonNotebookController controller = new PersonNotebookController();
        controller.run();
    }
}
